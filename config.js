module.exports = {
    token: ``, // The token of your bot
    prefix: `s!`, // The prefix for the commands of your bot
    guildId: ``, // The ID of your guild (for the support category)
    embedColor: ``, // The color of your default embed
    closeEmbedColor: `` // The color of the embed when you close a ticket
}